import {autorun, decorate, observable, reaction} from "mobx";

class TestStore {
    loading = true;
    badge = null;
}

decorate(TestStore, {
    loading: observable,
    badge: observable,
});
export default TestStore;