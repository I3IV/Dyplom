import React from 'react';
import Input from '@material-ui/core/Input';

import './search-panel.css';

const SearchPanel = (props) => {
      return (
          <Input placeholder="Search restaurants" {...props}/>
    );
}

export default SearchPanel;
